/**
 * @typedef {Object} BlockmarkData
 * @property {string} id
 * @property {Record<string, string>} attributes
 */

/**
 * @typedef {import('mdast').Nodes & { data?: { blockmark?: BlockmarkData } }} BlockmarkNode
 */

export {}
